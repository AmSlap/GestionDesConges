@model IEnumerable<GestionDesConges.Models.Approval>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LeaveRequest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Approver)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApprovalStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comments)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApprovalDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LeaveRequest.AbsenceType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Approver.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApprovalStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comments)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApprovalDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ApprovalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ApprovalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ApprovalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
